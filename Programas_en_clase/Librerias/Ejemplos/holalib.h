/* 
 * Header para holalib.c
 */

# ifndef HOLA_H // IF NOT DEFINE // Si la definicion HOLA_H no existe entra en el codigo. Se usa para no compilarlo dos veces.
# define HOLA_H

# include <stdio.h>
void hola(void);
#endif
