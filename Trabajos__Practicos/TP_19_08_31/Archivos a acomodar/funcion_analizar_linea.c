float* AnalizarLinea (char *linea){

    float buf[10];
    del[2]="/";
    char *p;
    int i = 0;

    p = strtok(linea,del);

    while(p!=NULL){

        buf[i] = atof[p];
        p = strtok(NULL,del);
        i++;

    }

    f1 = fopen()
    f2 = fopen()

}
